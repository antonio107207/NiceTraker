<% if notifications.any? %>
  <%= render partial: "notifications/notification", collection: notifications, as: :notification %>
<% else %>
  <p class="text-sm text-gray-400 text-center py-8"><%= t("notifications.empty") %></p>
<% end %>
