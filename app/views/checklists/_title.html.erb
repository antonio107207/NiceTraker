<h4 id="checklist_title_<%= checklist.id %>"
    class="text-sm font-semibold text-gray-800 flex items-center gap-1 cursor-text rounded px-0.5 -mx-0.5 hover:bg-gray-100 transition-colors focus:outline-none"
    data-controller="editable"
    data-editable-url-value="<%= checklist_path(checklist) %>"
    data-editable-field-value="checklist[title]">
  ☑ <%= checklist.title %>
</h4>
