<% content_for :title, t("profile.title") %>

<div class="min-h-screen bg-gray-50 py-10 px-4">
  <div class="max-w-2xl mx-auto">

    <%# Header %>
    <div class="mb-8">
      <h1 class="text-2xl font-bold text-gray-900"><%= t("profile.title") %></h1>
      <p class="text-sm text-gray-500 mt-1"><%= t("profile.subtitle") %></p>
    </div>

    <%# Avatar + Name card %>
    <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-6 mb-6">
      <div class="flex items-center gap-5 mb-6">
        <% if current_user.avatar.attached? %>
          <img src="<%= url_for(current_user.avatar) %>" alt="<%= current_user.display_name %>"
               class="w-20 h-20 rounded-full object-cover ring-4 ring-indigo-100">
        <% elsif current_user.avatar_url.present? %>
          <img src="<%= current_user.avatar_url %>" alt="<%= current_user.display_name %>"
               class="w-20 h-20 rounded-full object-cover ring-4 ring-indigo-100">
        <% else %>
          <div class="w-20 h-20 rounded-full bg-indigo-500 flex items-center justify-center text-white text-2xl font-bold ring-4 ring-indigo-100">
            <%= current_user.initials %>
          </div>
        <% end %>
        <div>
          <p class="text-lg font-semibold text-gray-900"><%= current_user.display_name %></p>
          <p class="text-sm text-gray-400"><%= current_user.email %></p>
        </div>
      </div>
    </div>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name),
          html: { method: :put, class: "space-y-6",
                  data: { controller: "profile-form", turbo: "false" } }) do |f| %>

      <%# Validation errors %>
      <% if resource.errors.any? %>
        <div class="bg-red-50 border border-red-200 rounded-xl px-4 py-3">
          <p class="text-sm font-medium text-red-700 mb-1">
            <%= t("errors.messages.not_saved", count: resource.errors.count,
                  resource: resource.class.model_name.human.downcase) %>
          </p>
          <ul class="list-disc list-inside text-sm text-red-600 space-y-0.5">
            <% resource.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%# Profile info section %>
      <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-6 space-y-5">
        <h2 class="text-sm font-semibold text-gray-700 uppercase tracking-wider"><%= t("profile.section_info") %></h2>

        <div>
          <%= f.label :name, t("profile.name"), class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.text_field :name,
                class: "w-full px-4 py-2.5 text-sm border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 focus:outline-none transition",
                autocomplete: "name" %>
        </div>

        <div>
          <%= f.label :email, t("profile.email"), class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.email_field :email,
                class: "w-full px-4 py-2.5 text-sm border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 focus:outline-none transition",
                autocomplete: "email" %>
          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <p class="text-xs text-amber-600 mt-1"><%= t("profile.pending_reconfirmation", email: resource.unconfirmed_email) %></p>
          <% end %>
        </div>

        <div>
          <%= f.label :avatar, t("profile.avatar"), class: "block text-sm font-medium text-gray-700 mb-2" %>

          <div data-controller="avatar-cropper" class="flex items-center gap-4">
            <%# Small circular preview %>
            <% if current_user.avatar.attached? %>
              <img data-avatar-cropper-target="preview"
                   src="<%= url_for(current_user.avatar) %>"
                   alt="" class="w-14 h-14 rounded-full object-cover ring-2 ring-indigo-200 flex-shrink-0">
            <% elsif current_user.avatar_url.present? %>
              <img data-avatar-cropper-target="preview"
                   src="<%= current_user.avatar_url %>"
                   alt="" class="w-14 h-14 rounded-full object-cover ring-2 ring-indigo-200 flex-shrink-0">
            <% else %>
              <img data-avatar-cropper-target="preview"
                   src="" alt="" class="hidden w-14 h-14 rounded-full object-cover ring-2 ring-indigo-200 flex-shrink-0">
              <div data-avatar-cropper-target="initials"
                   class="w-14 h-14 rounded-full bg-indigo-400 flex items-center justify-center text-white text-lg font-bold ring-2 ring-indigo-200 flex-shrink-0">
                <%= current_user.initials %>
              </div>
            <% end %>

            <div>
              <%= f.file_field :avatar,
                    accept: "image/*",
                    class: "hidden",
                    data: { avatar_cropper_target: "input", action: "change->avatar-cropper#onFileChange" } %>
              <button type="button"
                      data-action="click->avatar-cropper#openPicker"
                      class="px-4 py-2 text-sm font-semibold text-white bg-indigo-600 hover:bg-indigo-500 rounded-lg shadow-sm transition-colors cursor-pointer">
                <%= t("profile.change_photo") %>
              </button>
              <p class="text-xs text-gray-400 mt-1.5">JPG, PNG · <%= t("profile.photo_size_hint") %></p>
            </div>

            <%# Crop modal %>
            <div data-avatar-cropper-target="modal"
                 class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/50 backdrop-blur-sm p-4">
              <div class="glass-panel bg-white/90 backdrop-blur-2xl rounded-2xl shadow-2xl shadow-gray-900/25 w-full max-w-xs overflow-hidden">
                <div class="px-5 py-4 border-b border-gray-100">
                  <h3 class="text-base font-semibold text-gray-900"><%= t("profile.crop_photo") %></h3>
                  <p class="text-xs text-gray-400 mt-0.5"><%= t("profile.crop_hint") %></p>
                </div>
                <div class="p-1 bg-gray-900">
                  <canvas data-avatar-cropper-target="canvas"
                          width="320" height="320"
                          class="w-full aspect-square cursor-move touch-none block">
                  </canvas>
                </div>
                <div class="px-4 py-4 flex gap-3">
                  <button type="button"
                          data-action="click->avatar-cropper#cancel"
                          class="flex-1 py-2.5 text-sm font-medium text-gray-700 bg-gray-100 hover:bg-gray-200 rounded-xl transition-colors cursor-pointer">
                    <%= t("common.cancel") %>
                  </button>
                  <button type="button"
                          data-action="click->avatar-cropper#apply"
                          class="flex-1 py-2.5 text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 rounded-xl transition-colors cursor-pointer">
                    <%= t("profile.apply") %>
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <%# Password section %>
      <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-6 space-y-5">
        <h2 class="text-sm font-semibold text-gray-700 uppercase tracking-wider"><%= t("profile.section_password") %></h2>

        <div>
          <%= f.label :password, t("profile.new_password"), class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.password_field :password,
                autocomplete: "new-password",
                placeholder: t("profile.password_placeholder"),
                class: "w-full px-4 py-2.5 text-sm border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 focus:outline-none transition",
                data: { profile_form_target: "newPassword", action: "input->profile-form#onNewPasswordInput" } %>
          <% if @minimum_password_length %>
            <p class="text-xs text-gray-400 mt-1"><%= t("profile.min_length", count: @minimum_password_length) %></p>
          <% end %>
        </div>

        <div>
          <%= f.label :password_confirmation, t("profile.password_confirmation"), class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.password_field :password_confirmation,
                autocomplete: "new-password",
                placeholder: t("profile.password_placeholder"),
                class: "w-full px-4 py-2.5 text-sm border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 focus:outline-none transition",
                data: { profile_form_target: "newPassword", action: "input->profile-form#onNewPasswordInput" } %>
        </div>

        <div class="hidden border-t border-gray-100 pt-4" data-profile-form-target="currentPasswordSection">
          <%= f.label :current_password, t("profile.current_password"), class: "block text-sm font-medium text-gray-700 mb-1" %>
          <%= f.password_field :current_password,
                autocomplete: "current-password",
                class: "w-full px-4 py-2.5 text-sm border border-gray-300 rounded-xl focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 focus:outline-none transition" %>
          <p class="text-xs text-gray-400 mt-1"><%= t("profile.current_password_hint") %></p>
        </div>
      </div>

      <%# Actions %>
      <div class="flex items-center gap-3">
        <%= f.submit t("profile.save_btn"),
              class: "bg-indigo-600 hover:bg-indigo-700 text-white text-sm font-semibold px-6 py-2.5 rounded-xl cursor-pointer transition-colors" %>
        <%= link_to t("common.cancel"), :back,
              class: "px-6 py-2.5 text-sm font-semibold text-gray-600 bg-gray-100 hover:bg-gray-200 rounded-xl transition-colors" %>
      </div>
    <% end %>

    <%# Danger zone %>
    <div class="bg-white rounded-2xl shadow-sm border border-red-100 p-6 mt-6">
      <h2 class="text-sm font-semibold text-red-600 uppercase tracking-wider mb-3"><%= t("profile.danger_zone") %></h2>
      <p class="text-sm text-gray-500 mb-4"><%= t("profile.delete_hint") %></p>
      <%= button_to t("profile.delete_btn"),
            registration_path(resource_name),
            method: :delete,
            data: { turbo_confirm: t("profile.delete_confirm") },
            class: "bg-red-50 hover:bg-red-100 text-red-700 text-sm font-medium px-5 py-2 rounded-xl border border-red-200 cursor-pointer transition-colors" %>
    </div>

  </div>
</div>
